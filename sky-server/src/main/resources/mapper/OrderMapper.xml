<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders(number, user_id, status, address_book_id, order_time, checkout_time, pay_method, pay_status,
                           amount, remark, phone, address, consignee,estimated_delivery_time, delivery_status,
                           pack_amount, tableware_number,tableware_status)
        VALUES(#{number}, #{userId}, #{status}, #{addressBookId}, #{orderTime}, #{checkoutTime}, #{payMethod}, #{payStatus},
               #{amount}, #{remark}, #{phone}, #{address}, #{consignee}, #{estimatedDeliveryTime}, #{deliveryStatus},
               #{packAmount}, #{tablewareNumber}, #{tablewareStatus})
    </insert>
    <update id="update">
        update orders set number = #{number},
                          user_id = #{userId},
                          status = #{status},
                          address_book_id = #{addressBookId},
                          order_time = #{orderTime},
                          checkout_time = #{checkoutTime},
                          pay_method = #{payMethod},
                          pay_status = #{payStatus},
                          amount = #{amount},
                          remark = #{remark},
                          phone = #{phone},
                          address = #{address},
                          consignee = #{consignee},
                          estimated_delivery_time = #{estimatedDeliveryTime},
                          delivery_status = #{deliveryStatus},
                          pack_amount = #{packAmount},
                          tableware_number = #{tablewareNumber},
                          tableware_status = #{tablewareStatus}
        where id = #{id}

    </update>
    <select id="pageQuery" resultType="Orders">
        select * from orders
        <where>
            <if test="number != null and number!=''">
                and number like concat('%',#{number},'%')
            </if>
            <if test="phone != null and phone!=''">
                and phone like concat('%',#{phone},'%')
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="beginTime != null">
                and order_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and order_time &lt;= #{endTime}
            </if>
        </where>
        order by order_time desc
    </select>
</mapper>
